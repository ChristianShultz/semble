#
# Recurse down into lib (to build the library)
#                   includes (to install includes)
#                   main (build main programs)
#
SUBDIRS = lib test_lib

#
# The end user's configuration script
# 
bin_SCRIPTS = semble-config

#
# All the other goodies
#
#EXTRA_DIST = docs